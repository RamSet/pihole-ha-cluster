# Generated by docker-install.sh — 2026-03-02 19:57:07
services:
  pihole:
    image: pihole/pihole:latest
    network_mode: host
    cap_add:
      - NET_ADMIN
      - NET_RAW
      - SYS_NICE
    entrypoint: /pihole-ha-inject/pihole-ha-inject-docker.sh
    volumes:
      - pihole-etc:/etc/pihole
      - pihole-dnsmasq:/etc/dnsmasq.d
      - pihole-ha-src:/pihole-ha-src:ro
      - pihole-ha-run:/run/pihole-ha:ro
      - pihole-ha-conf:/etc/pihole-ha:ro
      - pihole-ha-ieee:/var/lib/ieee-data:ro
      - ./pihole-ha-inject-docker.sh:/pihole-ha-inject/pihole-ha-inject-docker.sh:ro
    environment:
      TZ: ${TZ:-America/New_York}
      FTLCONF_webserver_api_password: ${PIHOLE_PASSWORD:-}
      FTLCONF_webserver_port: ${PIHOLE_WEB_PORT:-80}
    restart: unless-stopped

  pihole-ha:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    network_mode: host
    pid: "service:pihole"
    cap_add:
      - NET_ADMIN
      - NET_RAW
    depends_on:
      - pihole
    env_file: .env
    volumes:
      - pihole-etc:/etc/pihole
      - pihole-dnsmasq:/etc/dnsmasq.d
      - pihole-ha-conf:/etc/pihole-ha
      - pihole-ha-run:/run/pihole-ha
      - pihole-ha-src:/pihole-ha-src
      - pihole-ha-ieee:/var/lib/ieee-data
    restart: unless-stopped

volumes:
  pihole-etc:
    external: true
  pihole-dnsmasq:
    external: true
  pihole-ha-conf:
  pihole-ha-run:
  pihole-ha-src:
  pihole-ha-ieee:
